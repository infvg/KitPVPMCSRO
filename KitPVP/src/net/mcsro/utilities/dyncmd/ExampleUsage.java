package net.mcsro.utilities.dyncmd;

//import net.mcsro.utils.dyncmd.command.DynCmd;
//import net.mcsro.utils.dyncmd.command.CommandManager;
//import net.mcsro.utils.dyncmd.command.objects.CommandInfo;

public class ExampleUsage
{
/*
    @Override
    public void onEnable()
    {
        CommandManager manager = new CommandManager(this);
        manager.registerCommands();
    }

    @DynCmd(command = "test")
    public void onTest(CommandInfo info)
    {
        System.out.println("Command: " + info.getCommand());
        System.out.println("Arg amounts: " + info.getArgsLength() + "\n Args:");
        for (String s : info.getArgs())
        {
            System.out.println(s);
        }
    }
*/
}